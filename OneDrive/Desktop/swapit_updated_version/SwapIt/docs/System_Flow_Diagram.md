# SwapIt - System Flow Diagram

## Complete System Architecture Flow

This document illustrates how all pages, functions, and components of the SwapIt system interact with each other.

---

## High-Level System Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER ENTRY POINT                         │
│                      http://localhost:8080                       │
└─────────────────────────────────────────────────────────────────┘
                              ↓
                    ┌─────────────────┐
                    │   home.html     │
                    │  (Landing Page) │
                    └─────────────────┘
                         ↓       ↓
              ┌──────────┘       └──────────┐
              ↓                              ↓
    ┌─────────────────┐          ┌─────────────────┐
    │   login.html    │          │  signup.html    │
    │                 │          │                 │
    └─────────────────┘          └─────────────────┘
              ↓                              ↓
              └──────────┐       ┌──────────┘
                         ↓       ↓
                  ┌─────────────────┐
                  │  Authentication  │
                  │   (auth.php)     │
                  └─────────────────┘
                         ↓
              ┌──────────┴──────────┐
              ↓                     ↓
     [Auth Failed]           [Auth Success]
           ↓                        ↓
    Return to Login      ┌─────────────────┐
                         │  dashboard.html │
                         │  (User Hub)     │
                         └─────────────────┘
```

---

## Detailed Page Flow Diagram

### 1. Guest User Journey

```
┌──────────────┐
│  home.html   │ ← Landing page with featured items
└──────────────┘
       ↓
       ├─→ Browse Items (browse.html) → View item details → [Login Required]
       ├─→ Login (login.html) → auth.php → Dashboard
       ├─→ Signup (signup.html) → auth.php → Profile Creation → Dashboard
       └─→ News Articles → Read only mode
```

### 2. Authenticated User Journey

```
┌──────────────────┐
│  dashboard.html  │ ← User's central hub
└──────────────────┘
       ↓
       ├─→ Browse Items (browse.html)
       │        ↓
       │        ├─→ View Item Details
       │        ├─→ Add to Cart (cart.js) → cart_items table
       │        ├─→ Add to Wishlist (saved_items table)
       │        └─→ Request Swap → swap_requests table
       │
       ├─→ My Profile (profile.html)
       │        ↓
       │        ├─→ Edit Profile Info → profiles table
       │        ├─→ View My Items
       │        ├─→ View My Reviews
       │        └─→ Change Password
       │
       ├─→ Add Listing (add-listing.html)
       │        ↓
       │        └─→ Create New Item → items table
       │
       ├─→ Shopping Cart (cart.html)
       │        ↓
       │        ├─→ Update Quantities
       │        ├─→ Remove Items
       │        └─→ Checkout → swap_requests
       │
       ├─→ Wishlist (wishlist.html)
       │        ↓
       │        ├─→ View Saved Items
       │        ├─→ Move to Cart
       │        └─→ Remove from Wishlist
       │
       └─→ Logout → Destroy Session → home.html
```

---

## Authentication Flow Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                      AUTHENTICATION SYSTEM                   │
│  Features: Email/Password + Social Login UI (OAuth pending) │
└─────────────────────────────────────────────────────────────┘

┌──────────────┐          ┌──────────────┐          ┌──────────────┐
│  Login Page  │          │  Signup Page │          │ Dashboard    │
│  (login.html)│          │(signup.html) │          │(dashboard)   │
│              │          │              │          │              │
│ - Email      │          │ - Name       │          │ - User Info  │
│ - Password   │          │ - Email      │          │ - Items      │
│ - Google*    │          │ - Password   │          │ - Requests   │
│ - Instagram* │          │ - Terms      │          │              │
│  (*disabled) │          │ - Google*    │          │              │
│              │          │ - Instagram* │          │              │
└──────────────┘          └──────────────┘          └──────────────┘
       ↓                          ↓                          ↑
       │ POST email/password      │ POST email/password/name │
       │ + Client validation      │ + Client validation      │
       ↓                          ↓                          │
┌────────────────────────────────────────────────────────────┐
│                      api/auth.php                          │
│                                                             │
│  ┌────────────────┐    ┌────────────────┐    ┌──────────┐│
│  │ Login Action   │    │ Signup Action  │    │  Logout  ││
│  │                │    │                │    │  Action  ││
│  │ 1. Validate    │    │ 1. Validate    │    │          ││
│  │ 2. Check DB    │    │ 2. Hash pwd    │    │ Destroy  ││
│  │ 3. Verify pwd  │    │ 3. Insert user │    │ Session  ││
│  │ 4. Create      │    │ 4. Create      │    │          ││
│  │    session     │    │    profile     │    │          ││
│  │ 5. Return JSON │    │ 5. Return JSON │    │          ││
│  └────────────────┘    └────────────────┘    └──────────┘│
│                                                             │
│  ┌─────────────────────────────────────────────────────┐  │
│  │          config/db.php (Database Connection)        │  │
│  │  • Host: localhost                                  │  │
│  │  • Database: SI2025                                 │  │
│  │  • User: root                                       │  │
│  │  • Password: (empty)                                │  │
│  └─────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────┘
                              ↓
                    ┌─────────────────┐
                    │  MySQL Database │
                    │     (SI2025)    │
                    │                 │
                    │  • users        │
                    │  • profiles     │
                    │  • sessions     │
                    └─────────────────┘
```

---

## Item Management Flow

```
┌───────────────────────────────────────────────────────────────┐
│                    ITEM MANAGEMENT SYSTEM                      │
└───────────────────────────────────────────────────────────────┘

1. LISTING CREATION
   ┌────────────────┐
   │ add-listing.   │
   │    html        │
   └────────────────┘
          ↓
   ┌────────────────┐
   │ add-listing.js │ → Validate form
   └────────────────┘    Upload images
          ↓               Category selection
   ┌────────────────┐
   │ API Endpoint   │ → Insert into items table
   └────────────────┘    Link to categories
          ↓
   ┌────────────────┐
   │ Database       │
   │ • items        │
   │ • categories   │
   └────────────────┘

2. BROWSING ITEMS
   ┌────────────────┐
   │  browse.html   │
   └────────────────┘
          ↓
   ┌────────────────┐
   │   browse.js    │ → Fetch items from API
   └────────────────┘    Apply filters
          ↓               Display grid
   ┌────────────────┐
   │ API Endpoint   │ → SELECT items + categories
   └────────────────┘    JOIN with users
          ↓               WHERE status='available'
   ┌────────────────┐
   │ Database       │
   │ • items        │
   │ • categories   │
   │ • users        │
   └────────────────┘

3. ITEM INTERACTION
   ┌──────────────────────────────────────┐
   │         Item Detail View             │
   └──────────────────────────────────────┘
                ↓
    ┌───────────┼───────────┐
    ↓           ↓           ↓
┌────────┐  ┌────────┐  ┌────────┐
│Add to  │  │Add to  │  │Request │
│Cart    │  │Wishlist│  │ Swap   │
└────────┘  └────────┘  └────────┘
    ↓           ↓           ↓
┌─────────┐ ┌──────────┐ ┌──────────┐
│cart_    │ │saved_    │ │swap_     │
│items    │ │items     │ │requests  │
└─────────┘ └──────────┘ └──────────┘
```

---

## Shopping Cart & Checkout Flow

```
┌───────────────────────────────────────────────────────────┐
│                  CART & CHECKOUT SYSTEM                    │
└───────────────────────────────────────────────────────────┘

Step 1: Add to Cart
┌──────────────┐
│ Browse Items │
└──────────────┘
       ↓
  Click "Add to Cart"
       ↓
┌──────────────┐
│  cart.js     │ → POST /api/cart.php
└──────────────┘    {action: 'add', item_id: X}
       ↓
┌──────────────┐
│  Database    │ → INSERT INTO cart_items
│              │   (user_id, item_id, quantity)
└──────────────┘

Step 2: View Cart
┌──────────────┐
│  cart.html   │
└──────────────┘
       ↓
┌──────────────┐
│  cart.js     │ → GET /api/cart.php
└──────────────┘    {action: 'get'}
       ↓
┌──────────────┐
│  Database    │ → SELECT cart_items
│              │   JOIN items
│              │   WHERE user_id = current_user
└──────────────┘
       ↓
Display cart items with:
• Item details
• Quantity controls
• Remove buttons
• Total calculation

Step 3: Checkout
┌──────────────┐
│ cart.html    │ → Click "Checkout"
└──────────────┘
       ↓
┌──────────────┐
│cart-checkout │ → For each cart item:
│    .js       │   Create swap_request
└──────────────┘    Clear cart
       ↓
┌──────────────┐
│  Database    │ → INSERT INTO swap_requests
│              │   DELETE FROM cart_items
└──────────────┘
       ↓
┌──────────────┐
│ Confirmation │ → Show success message
│    Page      │   Display request IDs
└──────────────┘
```

---

## Swap Request Workflow

```
┌─────────────────────────────────────────────────────────────┐
│                   SWAP REQUEST LIFECYCLE                     │
└─────────────────────────────────────────────────────────────┘

[REQUESTER SIDE]                    [OWNER SIDE]

1. Create Request                   1. Receive Notification
   ↓                                   ↓
┌──────────────┐                  ┌──────────────┐
│   Status:    │                  │   Status:    │
│   PENDING    │ ───────────────→ │   PENDING    │
└──────────────┘                  └──────────────┘
                                         ↓
                                  Review Request
                                         ↓
                              ┌──────────┴──────────┐
                              ↓                     ↓
                      ┌──────────────┐      ┌──────────────┐
                      │   Status:    │      │   Status:    │
                      │  ACCEPTED    │      │  REJECTED    │
                      └──────────────┘      └──────────────┘
                              ↓                     ↓
                       Arrange Pickup          End Process
                              ↓
                     Complete Exchange
                              ↓
                      ┌──────────────┐
                      │   Status:    │
                      │  COMPLETED   │
                      └──────────────┘
                              ↓
                       Leave Review
                              ↓
                      ┌──────────────┐
                      │   reviews    │
                      │    table     │
                      └──────────────┘

Database Updates at Each Stage:
• PENDING    → Initial creation
• ACCEPTED   → Owner approves
• REJECTED   → Owner declines
• COMPLETED  → Exchange finished
• CANCELLED  → Either party cancels
```

---

## Database Relationship Flow

```
┌──────────────────────────────────────────────────────────────┐
│                   DATABASE RELATIONSHIPS                      │
└──────────────────────────────────────────────────────────────┘

          ┌─────────┐
          │  users  │
          └─────────┘
               │
               │ 1:1
               ↓
          ┌──────────┐
          │ profiles │
          └──────────┘

          ┌─────────┐
          │  users  │ ───────┐
          └─────────┘        │
               │             │ owner_id
               │ 1:M         │
               ↓             ↓
          ┌─────────┐   ┌─────────┐
          │  items  │   │  items  │
          └─────────┘   └─────────┘
               │             │
        ┌──────┼──────┐      │
        │      │      │      │
     1:M│   1:M│   1:M│   1:M│
        ↓      ↓      ↓      ↓
  ┌────────┐ ┌────┐ ┌────┐ ┌────────┐
  │cart_   │ │swap│ │msg │ │saved_  │
  │items   │ │req │ │    │ │items   │
  └────────┘ └────┘ └────┘ └────────┘
      │         │
      │ M:1     │ 1:1
      ↓         ↓
  ┌─────────┐ ┌─────────┐
  │  users  │ │ reviews │
  └─────────┘ └─────────┘

  ┌────────────┐
  │ categories │
  └────────────┘
       │ 1:M
       ↓
  ┌─────────┐
  │  items  │
  └─────────┘
```

---

## JavaScript Module Interaction

```
┌──────────────────────────────────────────────────────────────┐
│                  FRONTEND JAVASCRIPT MODULES                  │
└──────────────────────────────────────────────────────────────┘

        ┌────────────────┐
        │   script.js    │ ← Main application logic
        │  (Universal)   │    Mobile menu toggle
        └────────────────┘    Navigation handling
                │
        ┌───────┼────────┐
        ↓       ↓        ↓
┌─────────┐ ┌──────┐ ┌──────────┐
│auth-    │ │cart  │ │dashboard │
│client.js│ │.js   │ │.js       │
└─────────┘ └──────┘ └──────────┘
     │          │          │
     └──────────┼──────────┘
                ↓
        ┌──────────────┐
        │  api/auth.php │
        └──────────────┘
                ↓
        ┌──────────────┐
        │   config/    │
        │   db.php     │
        └──────────────┘

Page-Specific Modules:
┌─────────────┐  ┌─────────────┐  ┌─────────────┐
│  login.js   │  │ signup.js   │  │ profile.js  │
│             │  │             │  │             │
│ • Validate  │  │ • Validate  │  │ • Load data │
│ • Submit    │  │ • Submit    │  │ • Update    │
│ • Redirect  │  │ • Create    │  │ • Save      │
└─────────────┘  └─────────────┘  └─────────────┘

┌─────────────┐  ┌─────────────┐  ┌─────────────┐
│  browse.js  │  │add-listing  │  │cart-checkout│
│             │  │    .js      │  │    .js      │
│ • Fetch     │  │ • Form      │  │ • Process   │
│ • Filter    │  │ • Upload    │  │ • Confirm   │
│ • Display   │  │ • Submit    │  │ • Clear     │
└─────────────┘  └─────────────┘  └─────────────┘
```

---

## Session Management Flow

```
┌──────────────────────────────────────────────────────────────┐
│                     SESSION LIFECYCLE                         │
└──────────────────────────────────────────────────────────────┘

User Visits Site
      ↓
┌─────────────┐
│PHP Session  │ → session_start()
│  Started    │   Check for existing session
└─────────────┘
      ↓
  Session Exists?
      ↓
  ┌───┴───┐
  │  Yes  │  No
  ↓       ↓
┌────┐  ┌──────────┐
│Load│  │ Create   │
│User│  │ New      │
│Data│  │ Session  │
└────┘  └──────────┘
  │           │
  └─────┬─────┘
        ↓
┌─────────────────┐
│ Session Data:   │
│ • user_id       │
│ • email         │
│ • full_name     │
│ • login_time    │
└─────────────────┘
        ↓
  User Activity
        ↓
┌─────────────────┐
│ Every Request:  │
│ • Validate      │
│ • Refresh       │
│ • Update        │
└─────────────────┘
        ↓
   User Logout
        ↓
┌─────────────────┐
│session_destroy()│
│ Clear all data  │
│ Redirect login  │
└─────────────────┘
```

---

## Error Handling Flow

```
┌──────────────────────────────────────────────────────────────┐
│                    ERROR HANDLING SYSTEM                      │
└──────────────────────────────────────────────────────────────┘

Frontend Validation
      ↓
┌─────────────┐     Pass      ┌─────────────┐
│  Validate   │ ───────────→  │  Send to    │
│  Input      │               │  Server     │
└─────────────┘               └─────────────┘
      │ Fail                         │
      ↓                              ↓
┌─────────────┐               Backend Validation
│  Show       │                     │
│  Error      │               ┌─────┴─────┐
│  Message    │               │           │
└─────────────┘          Pass │           │ Fail
                              ↓           ↓
                      ┌──────────┐  ┌──────────┐
                      │ Process  │  │  Return  │
                      │ Request  │  │  Error   │
                      └──────────┘  └──────────┘
                            │             │
                      ┌─────┴─────┐       │
                      │           │       │
                 Success│           │Error │
                      ↓           ↓       ↓
              ┌──────────┐  ┌──────────┐
              │  Return  │  │  Log     │
              │ Success  │  │  Error   │
              │  JSON    │  │  Return  │
              └──────────┘  │  JSON    │
                      ↓     └──────────┘
              ┌──────────┐       ↓
              │  Update  │  ┌──────────┐
              │  UI with │  │  Show    │
              │  Success │  │  Error   │
              └──────────┘  └──────────┘

Error Types:
• Validation Errors → Show inline
• Authentication Errors → Redirect to login
• Database Errors → Log and show generic message
• Network Errors → Retry or show connection message
```

---

## Complete User Story Flow

```
┌──────────────────────────────────────────────────────────────┐
│              COMPLETE USER JOURNEY EXAMPLE                    │
│          "Sarah wants to swap her textbook"                  │
└──────────────────────────────────────────────────────────────┘

1. Visit Website
   home.html → Sees featured items, reads about SwapIt

2. Create Account
   Click "Sign Up" → signup.html
   → Fill form → auth.php (action=signup)
   → Create user & profile → Success
   → Auto-login → dashboard.html

3. Create Listing
   Dashboard → Click "Add Listing"
   → add-listing.html
   → Fill details (title, description, category, condition, price)
   → Upload images
   → Submit → items table
   → Success → Back to dashboard

4. Browse Other Items
   Dashboard → Click "Browse"
   → browse.html → Load all items
   → Filter by "Books" category
   → See "Introduction to Python" book
   → View details

5. Add to Wishlist
   Item detail → Click "Save"
   → saved_items table
   → Confirmation message

6. Check Cart & Wishlist
   Dashboard → View saved items
   → Decide to request swap
   → Move item to cart

7. Checkout
   cart.html → Review items
   → Click "Checkout"
   → Create swap requests
   → swap_requests table (status: pending)

8. Wait for Response
   Dashboard → View "My Requests"
   → Status: Pending
   → Receive notification (owner accepted)

9. Complete Swap
   Meet owner → Exchange items
   → Mark as completed
   → swap_requests (status: completed)

10. Leave Review
    → reviews table
    → Rate experience
    → Write comment

DONE! 🎉
```

---

## System Monitoring Dashboard Flow

```
┌──────────────────────────────────────────────────────────────┐
│                    ADMIN/MONITORING VIEW                      │
└──────────────────────────────────────────────────────────────┘

test-db.html (Database Test Page)
      ↓
┌─────────────────┐
│ api/test-db.php │
└─────────────────┘
      ↓
Check:
┌────────────────────────────┐
│ • Connection Status        │
│ • Database Exists          │
│ • Tables Present           │
│ • Row Counts              │
│ • Errors/Warnings         │
└────────────────────────────┘
      ↓
Display Results:
✅ Database: Connected
✅ Tables: 9 found
✅ Status: Operational
```

---

This comprehensive flow diagram shows how every component of the SwapIt system interconnects and communicates, providing a complete picture of the system architecture and user journey.

**Document Version**: 1.0  
**Last Updated**: November 21, 2025
